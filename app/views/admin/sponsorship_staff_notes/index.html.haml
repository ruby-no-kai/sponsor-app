- content_for(:title) { t('.title') }

%nav{aria: {label: 'breadcrumb'}}
  %ol.breadcrumb
    %li.breadcrumb-item= link_to @conference.name, conference_path(@conference)
    %li.breadcrumb-item Sponsorships
    %li.breadcrumb-item= link_to @sponsorship.name, [@conference, @sponsorship]
    %li.breadcrumb-item.active{aria: {current: 'page'}} Staff Notes

%h5 Staff Notes

= render 'form', staff_note: @new_staff_note

- @staff_notes.each do |note|
  %section.my-2
    .card
      .card-body
        = render 'item', staff_note: note
