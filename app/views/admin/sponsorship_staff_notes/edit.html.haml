- content_for(:title) { t('.title') }

%nav{aria: {label: 'breadcrumb'}}
  %ol.breadcrumb
    %li.breadcrumb-item= link_to @conference.name, conference_path(@conference)
    %li.breadcrumb-item Sponsorships
    %li.breadcrumb-item= link_to @sponsorship.name, [@conference, @sponsorship]
    %li.breadcrumb-item= link_to "Staff Notes", [@conference, @sponsorship]
    %li.breadcrumb-item.active{aria: {current: 'page'}} Edit

= render 'form', staff_note: @staff_note

