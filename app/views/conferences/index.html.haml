- content_for(:title) { t('.title') }

.row.py-2

  - @conferences.each do |conference|
    .col-md-6
      .card
        .card-body
          = link_to conference.name, user_conference_sponsorship_path(conference)
